# direnv
eval "$(direnv hook zsh)"

# gh
eval "$(gh completion -s zsh)"

export GOPATH=$HOME
export PATH=$PATH:$GOPATH/bin

alias intra='awsid | jq -r ".Account" | pbcopy'

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

. /usr/local/opt/asdf/asdf.sh


